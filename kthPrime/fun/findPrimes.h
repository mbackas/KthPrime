#ifndef FINDPRIMESH
#define FINDPRIMESH

#include <vector>

unsigned long long findPrimes(const vector<unsigned long long>*& primes, vector<unsigned long long>::iterator& it, unsigned long long query);

unsigned long long kthPrime(int k);
#endif
